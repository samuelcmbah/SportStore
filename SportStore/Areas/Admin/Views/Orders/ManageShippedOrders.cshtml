@model IQueryable<Order>

@{
    ViewData["Title"] = "Shipped Orders";
}

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Date</th>
            <th>Name</th>
            <th>Product Details</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            @foreach (var order in Model)
            {
                <tr>
                    <td>@order.OrderDate</td>
                    <td>@order.Name</td>
                    <td>
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Product</th>
                                    <th>Quantity</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var product in order.OrderItems)
                                {
                                    <tr>
                                        <td>@product.Product.Name</td>
                                        <td>@product.Quantity</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </td>
                    <td>
                        <a class="btn btn-primary" readonly>Shipped</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr><td>There are no shipped orders at the moment.</td></tr>
        }
    </tbody>
</table>


